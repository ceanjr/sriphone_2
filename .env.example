################################################################################
# CONFIGURAÇÃO DE AMBIENTE - Sr. IPHONE 2
################################################################################
# Este arquivo documenta todas as variáveis de ambiente necessárias para o
# funcionamento do sistema. Copie este arquivo para .env.local e preencha
# com os valores reais do seu projeto.
#
# IMPORTANTE: Nunca commite o arquivo .env.local no git!
################################################################################

# ==============================================================================
# SUPABASE - Backend as a Service (Banco de dados + Autenticação)
# ==============================================================================
# Como obter: https://app.supabase.com/project/YOUR_PROJECT/settings/api
#
# NEXT_PUBLIC_* = Variáveis expostas no browser (podem ser públicas)
# Sem prefixo = Variáveis server-side apenas (devem ser secretas)

# URL do seu projeto Supabase
# Exemplo: https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Chave anônima pública do Supabase (pode ser exposta no browser)
# Usada para autenticação e queries públicas
# Exemplo: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Chave de serviço do Supabase (NUNCA exponha no browser!)
# Usada apenas em server-side para operações administrativas
# Tem permissões totais - mantenha em segredo!
# Exemplo: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ==============================================================================
# CLOUDINARY - Serviço de hospedagem e otimização de imagens
# ==============================================================================
# Como obter: https://cloudinary.com/console/settings/security
#
# Usado para upload e armazenamento das imagens dos produtos

# Nome do seu cloud Cloudinary
# Exemplo: dz1abc23xyz
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name

# API Key do Cloudinary (usado no server-side)
# Exemplo: 123456789012345
CLOUDINARY_API_KEY=your_cloudinary_api_key

# API Secret do Cloudinary (NUNCA exponha no browser!)
# Usado para autenticar uploads e deletes
# Exemplo: AbCdEfGhIjKlMnOpQrStUvWxYz
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ==============================================================================
# SITE - Configurações gerais da aplicação
# ==============================================================================

# URL base do site (usado para redirects e canonical URLs)
# Desenvolvimento: http://localhost:3000
# Produção: https://seu-dominio.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

################################################################################
# NOTAS IMPORTANTES
################################################################################
#
# 1. SEGURANÇA:
#    - Variáveis sem NEXT_PUBLIC_ são server-only e NUNCA expostas no browser
#    - Variáveis com NEXT_PUBLIC_ são bundled no JavaScript do cliente
#    - NUNCA commite .env.local, .env.production, ou arquivos com valores reais
#
# 2. AMBIENTES:
#    - .env.local = Desenvolvimento local (ignorado pelo git)
#    - .env.production = Produção (configurar na plataforma de deploy)
#    - .env.example = Template de documentação (commitado no git)
#
# 3. DEPLOY:
#    - Vercel: Configure as variáveis em Project Settings > Environment Variables
#    - Netlify: Configure em Site Settings > Environment Variables
#    - Docker: Use --env-file ou passe via docker-compose.yml
#
# 4. BACKUP:
#    - Mantenha backup seguro das credenciais Supabase e Cloudinary
#    - Considere usar um gerenciador de senhas (1Password, Bitwarden, etc)
#
################################################################################
