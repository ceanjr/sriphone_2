---
// src/components/Hero.astro (Tema Escuro)
---

<section class="hero">
  <!-- ✅ OTIMIZAÇÃO: Dimensões explícitas para prevenir CLS -->
  <img
    src="/images/Barbudo.webp"
    class="hero-img"
    alt="Sr. IPHONE Logo"
    fetchpriority="high"
    decoding="sync"
    loading="eager"
  />
  <h1>Seu iPhone, com <strong>Classe e Confiança</strong></h1>
  <p class="tagline">
    Revisamos, avaliamos e garantimos. Cada detalhe do processo assegura a
    entrega do melhor da tecnologia, com segurança e qualidade garantida.
  </p>
  <a href="#seminovos"
    ><img
      src="/icons/arrow-down.svg"
      alt=""
      class="arrow-down"
      loading="lazy"
    /></a
  >
</section>

<style>
  .hero {
    position: relative; /* necessário pra posicionar a seta dentro da seção */
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 2rem 1rem;
    height: calc(100vh - 120px);
    margin-bottom: 80px;
    justify-content: center;
    margin-top: 0;
  }

  .hero h1 {
    font-family: 'Halenoir', sans-serif;
    font-size: 2.2rem;
  }

  .hero-img {
    width: 50px; /* ajuste conforme necessário */
    max-width: 90vw;
    margin-bottom: 1.5rem;
    display: block;
  }

  .tagline {
    font-size: 1.1rem;
    margin-bottom: 2rem;
    color: var(--cor-texto);
  }

  .arrow-down {
    position: absolute;
    bottom: 20px; /* distância do fundo */
    left: 50%;
    transform: translateX(-50%);
    width: 38px;
    height: 38px;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateX(-50%) translateY(0);
    }
    40% {
      transform: translateX(-50%) translateY(-10px);
    }
    60% {
      transform: translateX(-50%) translateY(-5px);
    }
  }

  @media (min-width: 768px) {
    .hero {
      height: calc(100vh - 180px);
      margin-bottom: 140px;
      justify-content: center;
      margin-top: 0;
    }

    .hero h1 {
      font-size: 3.2rem;
    }

    .tagline {
      font-size: 1.3rem;
      max-width: 600px;
    }
  }
</style>
